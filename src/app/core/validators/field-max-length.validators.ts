import { Validators } from '@angular/forms';

export const NAME_MAX_LENGTH = Validators.maxLength(45);
export const FIELD_MAX_LENGTH = Validators.maxLength(250);
export const DESCRIPTION_MAX_LENGTH = Validators.maxLength(500);
